<div id="tracker_wrapper" class="span2">
  <h4>Tracker Option</h4>
  <% if user_signed_in? %>
    <label id="current_surah_label">
      <%= @form_player_option.sourates_list.key @form_player_option.surah_id.to_i %>
    </label>
    <div id="progress_current_surah" class="progress active">
        <div class="bar" width : <%= current_user.ayahs_knowns_per_surah @form_player_option.surah_id %></div>
    </div>
  <% end %>
    </div>
</div>

