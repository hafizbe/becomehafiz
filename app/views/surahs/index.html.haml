#surah_container{:class => 'row-fluid'}
  = render 'wrapper_options'
  #surah_wrapper_ar.span8.well
    -i = @from_verset_minimum.to_i
    -@versets.each do |verset|
      %div.verset_wrapper.dropdown
        -if @versets_traduit.nil?
          %div{:class => "verset_content dropdown-toggle arab_#{@size}", :id => "ayah_#{i}",  'data-placement' => 'none','data-toogle'=>'dropdown'}=verset
        -else
          %div{:class => "verset_content dropdown-toggle arab_#{@size}", :id => "ayah_#{i}",'modal_name' =>'myModal' ,'rel' => 'popover', 'data-placement' => 'right', 'data-traduction' => @versets_traduit[i-1]}=verset

        %ul.dropdown-menu
          %li
            %a
              %span.label.label-success
                Very good
          %li
            %a
              %span.label.label-warning
                Good
          %li
            %a
              %span.label.label-important
                Bad
          %li.divider
          %li
            %a
              %span.label.label-inverse
                Repete the verse

        -i = i + 1
      %span.break.hide="("+i.to_s+")"
